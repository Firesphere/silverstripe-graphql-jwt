validateToken:
  description: Validates a given token from the Bearer header
  type: MemberToken

validateResetToken:
  description: Validates a given reset token from the Bearer header
  type: AnonymousToken
